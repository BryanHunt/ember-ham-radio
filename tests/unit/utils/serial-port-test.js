import serialPort from 'ham-radio/utils/serial-port';
import { module, test } from 'qunit';

module('Unit | Utility | serial port');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = serialPort();
  assert.ok(result);
});
