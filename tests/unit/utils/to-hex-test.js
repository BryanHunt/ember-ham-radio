import toHex from 'ham-radio/utils/to-hex';
import { module, test } from 'qunit';

module('Unit | Utility | to hex');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toHex();
  assert.ok(result);
});
