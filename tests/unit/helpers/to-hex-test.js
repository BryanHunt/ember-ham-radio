import { toHex } from '../../../helpers/to-hex';
import { module, test } from 'qunit';

module('Unit | Helper | to hex');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toHex([42]);
  assert.ok(result);
});
